const Crud = aritfacts.requite('Crud');

contract("Crud", () => {
    let crud = null;
    before( async () => {
        crud = await Crud.deploy();
    });

    it('Should be create a player', () => {
        await 
    });
    

})